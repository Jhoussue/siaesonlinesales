export class Validacion{
    cantidad: number;
    existencia: number;
    exis: number;
    codigo: string;
  

    constructor(cantidad: number, existencia: number,codigo: string){
 this.cantidad=cantidad;
 this.existencia=existencia;
 this.codigo=codigo;

    }

}