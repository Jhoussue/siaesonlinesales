export class Vali{
    cantidad: number;
    existencia: number;
    exis: number;
    codigo: string;
  

    constructor(cantidad: number, existencia: number,codigo: string, exis: number){
 this.cantidad=cantidad;
 this.existencia=existencia;
 this.codigo=codigo;
 this.exis=exis;

    }

}